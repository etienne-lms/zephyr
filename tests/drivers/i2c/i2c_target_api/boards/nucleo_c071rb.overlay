/*
 * Copyright (c) 2025 STMicroelectronics
 * SPDX-License-Identifier: Apache-2.0
 */

/* I2C bus pins are exposed on the ST morpho header.
 *
 *  Bus        SDA               SCL
 *          Pin   Hdr         Pin   Hdr
 *  i2c1    PB9   CN5:9      PB8   CN5:10
 *  i2c2    PA6   CN5:5      PA7   CN5:4
 *
 * Short Pin PB9 to PA6, and PB8 to PA7, for the test to pass.
 */

&i2c1 {
	dmas = <&dmamux1 2 34 (STM32_DMA_PERIPH_TX | STM32_DMA_PRIORITY_HIGH)>,
	       <&dmamux1 3 33 (STM32_DMA_PERIPH_RX | STM32_DMA_PRIORITY_HIGH)>;
	dma-names = "tx", "rx";

	eeprom0: eeprom@54 {
		compatible = "zephyr,i2c-target-eeprom";
		reg = <0x54>;
		size = <256>;
	};
};

&i2c2 {
	pinctrl-0 = <&i2c2_scl_pa7 &i2c2_sda_pa6>;
	pinctrl-names = "default";
	scl-gpios = <&gpioa 7 GPIO_ACTIVE_HIGH>;
	sda-gpios = <&gpioa 6 GPIO_ACTIVE_HIGH>;
	dmas = <&dmamux1 4 36 (STM32_DMA_PERIPH_TX | STM32_DMA_PRIORITY_HIGH)>,
	       <&dmamux1 5 35 (STM32_DMA_PERIPH_RX | STM32_DMA_PRIORITY_HIGH)>;
	dma-names = "tx", "rx";
	status = "okay";
	clock-frequency = <I2C_BITRATE_FAST>;

	eeprom1: eeprom@56 {
		compatible = "zephyr,i2c-target-eeprom";
		reg = <0x56>;
		size = <256>;
	};
};
